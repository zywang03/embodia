robot:
  interface:
    name: vendor_robot
    images:
      front_rgb: rgb_front
    state:
      joint_positions: qpos
    action_modes:
      ee_delta: cartesian_delta
  method_aliases:
    observe: capture
    act: send_command
    reset: home

model:
  interface:
    name: vendor_model
    images:
      front_rgb: rgb_front
    state:
      joint_positions: qpos
    action_modes:
      ee_delta: cartesian_delta
    output_action_mode: ee_delta
  method_aliases:
    reset: clear_state
    step: infer
